# --- Nextio Photo Competition — environment ---
# Only needed for LOCAL testing with `vercel dev`.
# In production, set FOTORIVALS_TOKEN in Vercel's dashboard
# (Project → Settings → Environment Variables) — NOT in this file.
# NEVER commit your real .env file. It is already in .gitignore.

FOTORIVALS_TOKEN=replace-me-with-your-real-bearer-token
